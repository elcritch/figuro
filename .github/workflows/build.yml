on:
  pull_request:
  push:

jobs:
  tests:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        nimversion:
          - '2.x'

        os:
          - ubuntu-latest
          - macOS-latest
          - windows-latest
    steps:
    - uses: actions/checkout@v1
    - uses: iffy/install-nim@v4
      with:
        version: ${{ matrix.nimversion }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Install Deps
      run: |
        nim -v
        nimble -v
        nimble install 'https://github.com/nim-lang/atlas@#2ab291c9f37e9d7acce906d1bb7fa49f57f10b22'
        cp vendor/atlas.lock ./atlas.lock
        atlas replay atlas.lock

    - name: Cache packages
      uses: actions/cache@v3
      with:
        path: |
          vendor/
        key: ${{ runner.os }}-${{ hashFiles('vendor/atlas.lock') }}

    - name: Build Tests
      run: |
        nim test
